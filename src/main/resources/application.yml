spring:
  application:
    name:toDo
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/toDo-teach
    username: postgres
    password: root
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
    generate-ddl: true